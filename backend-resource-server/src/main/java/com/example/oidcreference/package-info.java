/**
 * Resource Server module. {@code @NullMarked}: every type-usage in this package
 * is non-null unless explicitly annotated {@code @Nullable}, so the deliberate
 * nullability (claim lookups that can miss, a nested-claim walk that can return
 * null) is carried in the type system rather than prose.
 */
@org.jspecify.annotations.NullMarked
package com.example.oidcreference;
