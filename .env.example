# Local development placeholders. The realm JSON ships dev secrets so the
# stack starts with no manual setup. Rotate these before any non-local use.

BFF_CLIENT_SECRET=dev-bff-secret-rotate-me
SERVICE_CLIENT_SECRET=dev-service-secret-rotate-me

# Optional overrides
# OIDC_ISSUER_URI=http://localhost:8080/realms/oidc-reference
# REDIS_HOST=localhost
# REDIS_PORT=6379
# RS_BASE_URL=http://localhost:8082
