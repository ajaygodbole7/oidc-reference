# Auth Service

Spring Boot service that owns the OAuth/OIDC client role for the browser
flow.

Primary Goal:

- `../docs/goals/GOAL-0004-auth-service.md`

Specification:

- `../docs/specs/SPEC-0001-core-oidc-flows.md` §Auth Service.

Implementation is delivered in `tasks/active/TASK-0008-auth-service-spec-to-code.md`.
This directory is the skeleton; no Java code lands until that task begins.
